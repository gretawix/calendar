{
    "name": "calendar",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "esbuild src/index.ts --bundle --outdir=dist --format=esm --platform=node --target=es2022 --sourcemap",
        "dev": "esbuild src/index.ts --bundle --outdir=dist --format=esm --target=es2022 --sourcemap --watch",
        "types": "tsc --watch --noEmit"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/gretawix/calendar.git"
    },
    "author": "",
    "license": "ISC",
    "bugs": {
        "url": "https://github.com/gretawix/calendar/issues"
    },
    "homepage": "https://github.com/gretawix/calendar#readme",
    "type": "module",
    "devDependencies": {
        "@types/node": "^22.1.0",
        "esbuild": "^0.23.0",
        "esbuild-plugin-ts": "^0.0.1-rev-1.1",
        "typescript": "^5.5.4"
    }
}
